import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row'
    },
    img: {
        width: 11.08,
        height: 11.08,
        marginLeft: 2
    },
    
});
export default styles;