import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 80
    },
    imgLogo: {
        width: 70,
        height: 70
    }
});
export default styles;